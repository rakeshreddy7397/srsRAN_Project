gnb_id: 412
gnb_du_id: 0

f1ap:
  cu_cp_addr: 127.0.10.3
  bind_addr: 127.0.10.4 

nru:
  queue_size: 2048
  bind_addr: 127.0.10.4 

ru_sdr:
  device_driver: zmq                # The RF driver name.
  device_args: tx_port=tcp://127.0.0.1:2400,rx_port=tcp://127.0.0.1:2401,base_srate=11.52e6 # Optionally pass arguments to the selected RF driver.
  srate: 11.52                      # RF sample rate might need to be adjusted according to selected bandwidth.
  tx_gain: 75                       # Transmit gain of the RF might need to adjusted to the given situation.
  rx_gain: 75                       # Receive gain of the RF might need to adjusted to the given situation.
  # device_driver: uhd
  # device_args: type=b200
  # tx_gain: 20
  # rx_gain: 15
  # srate: 23.04
  # lo_offset: 45
  # clock: internal
  # sync: internal
  # time_alignment_calibration: 0

cell_cfg:
  dl_arfcn: 375000                  # ARFCN of the downlink carrier (center frequency).
  band: 3                           # The NR band.
  channel_bandwidth_MHz: 10         # Bandwith in MHz. Number of PRBs will be automatically derived.
  common_scs: 15                    # Subcarrier spacing in kHz used for data.
  plmn: "00101"                     # PLMN broadcasted by the gNB.
  tac: 7                            # Tracking area code (needs to match the core configuration).
  pdcch:
    common:
      ss0_index: 0                  # Set search space zero index to match srsUE capabilities
      coreset0_index: 6             # Set search CORESET Zero index to match srsUE capabilities
    dedicated:
      ss2_type: common              # Search Space type, has to be set to common
      dci_format_0_1_and_1_1: false # Set correct DCI format (fallback)
  pci: 2
  sector_id: 1
  prach:
    prach_config_index: 1           # Sets PRACH config to match what is expected by srsUE

log:
  filename: /tmp/du2.log            # Path of the log file.
  all_level: debug                   # Logging level applied to all layers.

e2:
  enable_du_e2: true                # Enable DU E2 agent (one for each DU instance)
  e2sm_kpm_enabled: true            # Enable KPM service module
  e2sm_rc_enabled: true
  addr: 10.0.2.10                   # RIC IP address
  # [TODO: test consequence if have same bind addr]
  bind_addr: 10.0.2.1               # A local IP that the E2 agent binds to for traffic from the RIC. ONLY required if running the RIC on a separate machine. 
  port: 36421                       # RIC port
